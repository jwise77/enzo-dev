.. _MakeOptions:

The Enzo Build System
=====================

Enzo uses CMake to manage its build process across platforms.

- For current build options and CMake usage, see :ref:`CMakeOptions`.
- For historical documentation on the legacy ``Make.mach.*`` Makefile system, see :ref:`LegacyMakeOptions`.
